{
  "name": "remindarr",
  "version": "1.0.0",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "concurrently \"bun run dev:server\" \"bun run dev:frontend\"",
    "dev:server": "bun run --watch server/index.ts",
    "dev:frontend": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "start": "bun run server/index.ts",
    "sync": "bun run server/cli/sync.ts",
    "check": "bunx tsc --noEmit && cd frontend && bunx tsc --noEmit && cd .. && bun test",
    "test": "bun test",
    "test:server": "bun test server/",
    "test:frontend": "bun test frontend/src/",
    "db:generate": "bunx drizzle-kit generate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio",
    "deploy:cf": "npx wrangler d1 migrations apply remindarr --remote && npx wrangler deploy"
  },
  "dependencies": {
    "@sentry/bun": "^10.43.0",
    "better-auth": "^1.5.5",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.4.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.8.4",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@types/web-push": "^3.6.4",
    "concurrently": "^9.2.1",
    "drizzle-kit": "^0.31.9",
    "happy-dom": "^20.8.4"
  }
}
